node_modules/
*.log
*Zone.Identifier
.env

# Internal design doc — never publish (kept locally only).
PRD-switchboard.md

# Loose screenshots in the repo root (keep only assets/).
/*.png

# Tool/session detritus — never product code.
.agents/
.claude/
.context/
.playwright-mcp/
skills-lock.json
