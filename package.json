{
  "name": "switchboard",
  "version": "0.1.0",
  "private": true,
  "description": "Hub local que conecta instâncias independentes de Claude Code em sessões tmux, com troca assíncrona de mensagens via MCP e dashboard web de observação.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^15.0.0",
    "express": "^5.2.1",
    "ulid": "^3.0.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20",
    "tsx": "^4.23.0",
    "vitest": "^4.1.10"
  }
}
